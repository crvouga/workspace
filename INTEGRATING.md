# Integrating with shared infra — moved to `llms.txt`

The integration guide for other repos (Vault, Turborepo remote cache, R2 object
store, and Dockerized hosting on the managed fleet) is now the generated
[`llms.txt`](llms.txt) at the repo root. Fetch and follow it:

https://raw.githubusercontent.com/crvouga/workspace/main/llms.txt

This stub only exists so older links keep working. Point your project's
`AGENTS.md` / `CLAUDE.md` at the `llms.txt` URL above.
